client class Util2 {
    static void createCustomer() async {
        // Create a randomized customer
        Customer c =
            Customer(name: 'FirstCustomer', dob: DateTime.utc(1992, 3, 23));
        DBResult s = c.save().await;
    }

    static void createInvoiceItem() {
        // Create a randomized customer
    }
}